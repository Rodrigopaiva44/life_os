"""
domain/
=======
Camada de Domínio — o núcleo da Clean Architecture.

Contém:
  - models.py   → entidades SQLModel (schema canônico)
  - (futuro) value_objects.py  → objetos de valor imutáveis
  - (futuro) events.py         → domain events para o Agente de IA
"""
